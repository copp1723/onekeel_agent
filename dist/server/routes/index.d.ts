import { Express } from 'express';
/**
 * Register all auth and credential routes with the Express app
 * @param app Express application instance
 */
export declare function registerAuthRoutes(app: Express): Promise<void>;
