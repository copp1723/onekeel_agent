# AgentFlow Environment Variables
# This file contains all environment variables required for the AgentFlow application

# ===== DATABASE CONFIGURATION =====
# Required: PostgreSQL connection string for database access
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# Optional: Supabase project URL (if using Supabase)
SUPABASE_URL=https://your-project.supabase.co
# Optional: Supabase anonymous key (if using Supabase)
SUPABASE_ANON_KEY=your_supabase_anon_key

# ===== API KEYS =====
# Required: Eko API key for AI agent functionality
EKO_API_KEY=your_eko_api_key
# Required: OpenAI API key for insight generation
OPENAI_API_KEY=your_openai_api_key
# Optional: SendGrid API key for email sending
SENDGRID_API_KEY=your_sendgrid_api_key

# ===== EMAIL CONFIGURATION =====
# Required: Email account for sending notifications
EMAIL_USER=your_email_username
EMAIL_PASS=your_email_password
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
# Required: Email account for OTP retrieval
OTP_EMAIL_USER=your_email_username
OTP_EMAIL_PASS=your_email_password
OTP_PATTERN=OTP is: (\\d{6})
OTP_SUBJECT=Your OTP Code

# ===== CRM PLATFORM CREDENTIALS =====
# VinSolutions credentials
VIN_SOLUTIONS_USERNAME=your_vinsolutions_username
VIN_SOLUTIONS_PASSWORD=your_vinsolutions_password
# VAUTO credentials
VAUTO_USERNAME=your_vauto_username
VAUTO_PASSWORD=your_vauto_password

# ===== APPLICATION CONFIGURATION =====
# Optional: Node environment (development, production, test)
NODE_ENV=development
# Optional: Server port (default: 5000)
PORT=5000
# Optional: Log level (default: info)
LOG_LEVEL=info
# Optional: Download directory for reports
DOWNLOAD_DIR=./downloads

# ===== SECURITY CONFIGURATION =====
# Required in production: 32-byte key for AES-256-GCM encryption
ENCRYPTION_KEY=default-dev-key-should-change-in-production
# Optional: Security audit logging level (default: info)
SECURITY_AUDIT_LEVEL=info

# ===== HEALTH MONITORING =====
# Optional: Comma-separated list of admin email addresses for alerts
ADMIN_EMAILS=admin@example.com
# Optional: Health check interval in minutes (default: 15)
HEALTH_CHECK_INTERVAL=15